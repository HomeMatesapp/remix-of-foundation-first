/**
 * Version of the canonical Career Pack SCHEMA (the document framework itself).
 *
 * This is NOT:
 * - the content version of an individual authored Career Pack (`pack.version`),
 * - the Decision Engine version (Increment 9),
 * - the domain-contract version, question-module framework version or route
 *   archetype framework version.
 */
export const CAREER_PACK_SCHEMA_VERSION = "1.0.0" as const;
